<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pet.module.mall.mapper.MallCartMapper">

    <resultMap id="BaseResultMap" type="com.pet.module.mall.model.entity.MallCart">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="product_id" property="productId"/>
        <result column="quantity" property="quantity"/>
        <result column="price" property="price"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="BaseColumns">
        id, user_id, product_id, quantity, price, created_at, updated_at
    </sql>

    <insert id="upsert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO mall_cart (user_id, product_id, quantity, price)
        VALUES (#{userId}, #{productId}, #{quantity}, #{price})
        ON DUPLICATE KEY UPDATE quantity = quantity + #{quantity}, updated_at = NOW()
    </insert>

    <select id="selectByUserAndProduct" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM mall_cart
        WHERE user_id = #{userId} AND product_id = #{productId}
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM mall_cart
        WHERE user_id = #{userId} ORDER BY created_at ASC
    </select>

    <delete id="deleteByUserAndProduct">
        DELETE FROM mall_cart WHERE user_id = #{userId} AND product_id = #{productId}
    </delete>

    <delete id="deleteByUserId">
        DELETE FROM mall_cart WHERE user_id = #{userId}
    </delete>

</mapper>
