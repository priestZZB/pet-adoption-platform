# ============================================
# 前端 — 多阶段构建：Node 编译 → Nginx 运行
# ============================================

# ---- 阶段1：编译 ----
FROM node:22-alpine AS builder
WORKDIR /build

COPY package*.json ./
RUN npm ci

COPY . .
RUN npm run build

# ---- 阶段2：运行 ----
FROM nginx:alpine
COPY --from=builder /build/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
