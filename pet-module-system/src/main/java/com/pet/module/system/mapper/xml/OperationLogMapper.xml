<?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pet.module.system.mapper.OperationLogMapper">

    <resultMap id="BaseResultMap" type="com.pet.module.system.model.entity.SysOperationLog">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="module" property="module"/>
        <result column="action" property="action"/>
        <result column="ip" property="ip"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <sql id="BaseColumns">
        id, user_id, username, module, action, ip, created_at
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_operation_log (user_id, username, module, action, ip)
        VALUES (#{userId}, #{username}, #{module}, #{action}, #{ip})
    </insert>

    <select id="selectPage" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM sys_operation_log
        <where>
            <if test="module != null and module != ''">
                AND module = #{module}
            </if>
        </where>
        ORDER BY created_at DESC
    </select>
</mapper>